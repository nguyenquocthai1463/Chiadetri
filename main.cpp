#include <iostream>

using namespace std;

int tong(int a[],int l,int r){
    int mid=(l+r)/2;
    if(a[mid]%2==0){
        return
    }
}

int main() {
    int base, exponent;
    int sum;
    cin >> base;
    cin >> exponent;
    sum = power(base,exponent);
    cout<<sum;
    return 0;
}